import * as lodash from "lodash";

/** 
 * @example
 *
 * subtraction(2 , 2)
 * // => 0
 */
const subtraction = (minuend : number , subtrahend : number) => lodash.subtract(minuend , subtrahend); 

export default subtraction;